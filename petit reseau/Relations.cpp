#include <cmath>
#include "reseaux.h"


/*
double delta_(double whs, double prob, double val)
{
    double delta = val * (1 - val) * whs * prob;
    return delta;
};*/


///read info
//if (file.is_open())
//{
///	std::streampos size;
///	size = file.tellg();
///	file.seekg(0, std::ios::beg);
	//file.read(info , size_of(resaux));
///	file.close();
